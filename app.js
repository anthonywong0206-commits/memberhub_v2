const STORE_MEMBERS='memberhub_members_v1';
const STORE_SETTINGS='memberhub_settings_v1';
const $=s=>document.querySelector(s);
const app=$('#app');
const todayISO=()=>new Date().toISOString().slice(0,10);
const nowText=()=>new Date().toLocaleString('zh-HK',{hour12:false});
const uid=()=>Date.now().toString(36)+Math.random().toString(36).slice(2,8);
const escapeHtml=v=>String(v??'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));
const load=(k,fb)=>{try{return JSON.parse(localStorage.getItem(k))??fb}catch{return fb}};
const save=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
let state={page:'home',members:load(STORE_MEMBERS,[]),settings:load(STORE_SETTINGS,{companyName:'我的機構',password:'1234',memberPrefix:'MEM',startNumber:1,includeYear:true,includeMonth:false,currentSequence:1,manualMemberNumber:false}),draft:null,lockedSignature:null,viewMember:null,editMode:false,toast:''};
function persist(){save(STORE_MEMBERS,state.members);save(STORE_SETTINGS,state.settings)}
function nextMemberNumber(settings=state.settings){const seq=String(settings.currentSequence||settings.startNumber||1).padStart(4,'0');const y=settings.includeYear?new Date().getFullYear()+'-':'';const m=settings.includeMonth?String(new Date().getMonth()+1).padStart(2,'0')+'-':'';return `${settings.memberPrefix||'MEM'}-${y}${m}${seq}`}
function calcAge(d){if(!d)return'';const dob=new Date(d);if(isNaN(dob))return'';const now=new Date();let a=now.getFullYear()-dob.getFullYear();const md=now.getMonth()-dob.getMonth();if(md<0||(md===0&&now.getDate()<dob.getDate()))a--;return Math.max(0,a)}
function setPage(p){state.page=p;state.viewMember=null;state.editMode=false;state.toast='';render()}
function verifyPassword(msg='請輸入系統密碼'){const pass=prompt(msg);return pass!==null && pass===String(state.settings.password||'')}
function toast(t){state.toast=t;render();setTimeout(()=>{state.toast='';render()},1800)}
const fieldNames={memberNumber:'會員編號',name:'姓名',gender:'性別',dateOfBirth:'出生年月日',age:'年齡',hkid:'身份證號碼',phone:'電話號碼',email:'電郵地址',address:'住址',emergencyContactName:'緊急聯絡人姓名',emergencyContactPhone:'緊急聯絡人電話',healthDeclaration:'沒有重大疾病聲明',remarks:'備註',applicantName:'申請人姓名',signatureDate:'簽署日期'};
function memberToRows(m){return [
 ['會員編號',m.memberNumber],['姓名',m.name],['性別',m.gender],['出生年月日',m.dateOfBirth],['年齡',m.age],['身份證號碼',m.hkid],['電話號碼',m.phone],['電郵地址',m.email],['住址',m.address],['緊急聯絡人姓名',m.emergencyContactName],['緊急聯絡人電話',m.emergencyContactPhone],['備註',m.remarks||'—']
]}
function pdfHtml(m){const rows=memberToRows(m).map(([k,v])=>`<tr><th>${k}</th><td>${escapeHtml(v||'—')}</td></tr>`).join('');return `<div class="a4" id="pdfSheet"><div class="pdf-title"><h2>${escapeHtml(state.settings.companyName||'我的機構')}</h2><p>會員登記表 Membership Registration Form</p></div><table class="pdf-table"><tbody>${rows}</tbody></table><div class="declaration"><b>聲明：</b>本人聲明沒有已知重大疾病，並確認以上資料真確無誤。<br>聲明狀態：${m.healthDeclaration?'已確認':'未確認'}　登記日期：${escapeHtml(m.createdAt||nowText())}　最後更新：${escapeHtml(m.updatedAt||m.createdAt||nowText())}</div><div class="sign-box"><div class="sign-area"><b>申請人姓名：</b>${escapeHtml(m.applicantName||m.name||'')}<br><b>日期：</b>${escapeHtml(m.signatureDate||todayISO())}</div><div class="sign-area"><b>電子簽署：</b>${m.signatureImage?`<img src="${m.signatureImage}" alt="signature">`:'<p>未有電子簽署</p>'}</div></div><div class="footer-note">本表格由 MemberHub 會員管理系統產生</div></div>`}
async function downloadPdf(member){const filename=`會員登記表_${member.memberNumber}_${member.name||'會員'}_${todayISO()}.pdf`;let holder=document.createElement('div');holder.style.position='fixed';holder.style.left='-9999px';holder.style.top='0';holder.innerHTML=pdfHtml(member);document.body.appendChild(holder);const sheet=holder.querySelector('#pdfSheet');try{if(window.html2canvas&&window.jspdf){const canvas=await html2canvas(sheet,{scale:2,useCORS:true,backgroundColor:'#ffffff'});const img=canvas.toDataURL('image/jpeg',0.96);const {jsPDF}=window.jspdf;const pdf=new jsPDF('p','mm','a4');pdf.addImage(img,'JPEG',0,0,210,297);pdf.save(filename)}else{const w=window.open('','_blank');w.document.write(`<!doctype html><html><head><title>${filename}</title><link rel="stylesheet" href="style.css"></head><body>${pdfHtml(member)}<script>setTimeout(()=>window.print(),500)<\/script></body></html>`);w.document.close()}}catch(e){alert('PDF 產生失敗，請稍後再試或改用列印另存 PDF。');console.error(e)}finally{holder.remove()}}
function render(){app.innerHTML=`<div class="app"><header class="top"><div class="brand"><div class="logo">M</div><div><h1>MemberHub 會員管理系統</h1><p class="sub">快速登記、管理及匯出會員資料。</p></div></div><div class="pill">${escapeHtml(state.settings.companyName||'我的機構')} · ${todayISO()}</div></header>${state.toast?`<div class="notice success">${escapeHtml(state.toast)}</div>`:''}${pages[state.page]()}<nav class="nav"><div class="navinner">${['home:首頁','register:登記','members:會員','settings:設定'].map(x=>{const[a,b]=x.split(':');return`<button class="${state.page===a?'active':''}" onclick="setPage('${a}')">${b}</button>`}).join('')}</div></nav></div>`;afterRender()}
const pages={home(){const month=new Date().toISOString().slice(0,7);const thisMonth=state.members.filter(m=>(m.createdAtISO||'').slice(0,7)===month).length;const recent=[...state.members].slice(-5).reverse();return `<section class="card hero"><h2>歡迎使用</h2><p class="sub">本系統資料只儲存在此瀏覽器 localStorage。</p><div class="grid"><div class="card"><div class="stat">${state.members.length}</div><div class="label">會員總數</div></div><div class="card"><div class="stat">${thisMonth}</div><div class="label">本月新增</div></div><div class="card"><div class="stat">${state.settings.manualMemberNumber?'人手輸入':nextMemberNumber()}</div><div class="label">${state.settings.manualMemberNumber?'會員編號模式':'下一個會員編號'}</div></div></div><div class="actions"><button class="btn" onclick="setPage('register')">新會員登記</button><button class="btn ghost" onclick="setPage('members')">會員管理</button><button class="btn ghost" onclick="setPage('settings')">設定</button></div></section><section class="card"><h3>最近新增會員</h3>${recent.length?recent.map(m=>`<div class="member-row"><div><div class="member-name">${escapeHtml(m.name)}</div><div class="member-no">${escapeHtml(m.memberNumber)}</div></div></div>`).join(''):'<p class="sub">暫未有會員紀錄。</p>'}</section>`},
register(){const m=state.draft||{memberNumber:nextMemberNumber(),name:'',gender:'',dateOfBirth:'',age:'',hkid:'',phone:'',email:'',address:'',emergencyContactName:'',emergencyContactPhone:'',healthDeclaration:false,remarks:''};return `<section class="card"><h2>新會員登記</h2><p class="sub">填寫資料後按「確認」，進入 PDF 預覽及電子簽署流程。</p>${state.settings.manualMemberNumber?`<div class="notice warn">現正使用人手會員編號模式：請自行輸入會員編號，系統會檢查是否重複。</div>`:``}<form id="regForm" class="formgrid"><div class="field"><label>會員編號</label><input name="memberNumber" value="${escapeHtml(m.memberNumber)}" ${state.settings.manualMemberNumber?'':'readonly'} placeholder="請輸入會員編號"></div><div class="field"><label>姓名 <span class="req">*</span></label><input name="name" value="${escapeHtml(m.name)}" required></div><div class="field"><label>性別</label><select name="gender"><option value="">請選擇</option>${['男','女','其他/不透露'].map(v=>`<option ${m.gender===v?'selected':''}>${v}</option>`).join('')}</select></div><div class="field"><label>出生年月日 <span class="req">*</span></label><input type="date" name="dateOfBirth" value="${escapeHtml(m.dateOfBirth)}" required></div><div class="field"><label>年齡</label><input name="age" value="${escapeHtml(m.age)}" readonly></div><div class="field"><label>身份證號碼</label><input name="hkid" value="${escapeHtml(m.hkid)}"></div><div class="field"><label>電話號碼 <span class="req">*</span></label><input name="phone" inputmode="tel" value="${escapeHtml(m.phone)}" required></div><div class="field"><label>電郵地址</label><input type="email" name="email" value="${escapeHtml(m.email)}"></div><div class="field full"><label>住址</label><textarea name="address">${escapeHtml(m.address)}</textarea></div><div class="field"><label>緊急聯絡人姓名</label><input name="emergencyContactName" value="${escapeHtml(m.emergencyContactName)}"></div><div class="field"><label>緊急聯絡人電話</label><input name="emergencyContactPhone" inputmode="tel" value="${escapeHtml(m.emergencyContactPhone)}"></div><div class="field full"><label><input type="checkbox" name="healthDeclaration" ${m.healthDeclaration?'checked':''} style="width:auto;min-height:auto"> 本人聲明沒有已知重大疾病，並確認以上資料真確無誤。</label></div><div class="field full"><label>備註</label><textarea name="remarks">${escapeHtml(m.remarks)}</textarea></div><div class="field full actions"><button type="button" class="btn ghost" onclick="clearRegister()">清空表格</button><button class="btn" type="submit">確認</button></div></form></section>`},
preview(){const m=state.draft;return `<section class="card"><h2>PDF 表格預覽及電子簽署</h2><p class="sub">請先簽署，然後按「確認簽署」鎖定簽名。完成後可儲存會員及下載 PDF。</p><div class="preview-wrap">${pdfHtml({...m,signatureImage:state.lockedSignature||m.signatureImage})}</div><div class="card"><h3>電子簽署</h3><canvas id="sigPad" class="signature-pad"></canvas><div class="formgrid" style="margin-top:12px"><div class="field"><label>申請人姓名</label><input id="applicantName" value="${escapeHtml(m.applicantName||m.name||'')}"></div><div class="field"><label>日期</label><input id="signatureDate" type="date" value="${escapeHtml(m.signatureDate||todayISO())}"></div></div><div class="actions" style="margin-top:12px"><button class="btn ghost" onclick="clearSignature()">清除簽名</button><button class="btn soft" onclick="confirmSignature()">確認簽署</button><button class="btn ok" onclick="saveDraftMember()">儲存會員</button><button class="btn" onclick="downloadDraftPdf()">下載 PDF</button><button class="btn ghost" onclick="state.page='register';render()">返回修改</button></div>${state.lockedSignature?'<div class="notice success">簽名已確認，PDF 將顯示此電子簽署。</div>':'<div class="notice">尚未確認簽署，下載 PDF 前請按「確認簽署」。</div>'}</div></section>`},
members(){const q=(window._memberSearch||'').toLowerCase();const month=window._memberMonth||'';let list=[...state.members].reverse().filter(m=>!q||[m.name,m.memberNumber,m.phone,m.hkid].some(v=>String(v||'').toLowerCase().includes(q))).filter(m=>!month||(m.createdAtISO||'').slice(0,7)===month);const months=[...new Set(state.members.map(m=>(m.createdAtISO||'').slice(0,7)).filter(Boolean))].sort().reverse();return `<section class="card"><h2>會員管理</h2><p class="sub">列表只顯示姓名及會員編號；查看、修改及刪除均需密碼。</p><div class="searchbar"><input id="memberSearch" placeholder="搜尋姓名 / 會員編號 / 電話 / 身份證" value="${escapeHtml(window._memberSearch||'')}"><select id="memberMonth"><option value="">所有月份</option>${months.map(mm=>`<option value="${mm}" ${month===mm?'selected':''}>${mm}</option>`).join('')}</select></div>${list.length?list.map(m=>`<div class="member-row"><div class="member-main"><div class="member-name">${escapeHtml(m.name||'未命名')}</div><div class="member-no">${escapeHtml(m.memberNumber)} ${m.duplicateNumber?'<span class="badge-warn">編號重複</span>':''}</div></div><div class="actions"><button class="btn soft" onclick="openMember('${m.id}')">查看</button><button class="btn danger" onclick="deleteMember('${m.id}')">刪除</button></div></div>`).join(''):'<p class="sub">沒有會員紀錄。</p>'}</section>${state.viewMember?memberModal(state.viewMember):''}`},
settings(){const s=state.settings;return `<section class="card"><h2>設定</h2><form id="settingsForm" class="formgrid"><div class="field full"><label>公司名稱</label><input name="companyName" value="${escapeHtml(s.companyName)}"></div><div class="field"><label>系統密碼</label><input name="password" value="${escapeHtml(s.password)}"></div><div class="field full"><label class="checkline"><input type="checkbox" name="manualMemberNumber" ${s.manualMemberNumber?'checked':''}> 啟用人手更改會員編號</label><p class="mini">勾選後，登記及修改會員資料時可自行輸入會員編號；取消勾選後會按以下格式自動編號。</p></div><div class="field"><label>會員編號前綴</label><input name="memberPrefix" value="${escapeHtml(s.memberPrefix)}"></div><div class="field"><label>起始 / 下一流水號</label><input type="number" name="currentSequence" min="1" value="${escapeHtml(s.currentSequence)}"></div><div class="field"><label>加入年份</label><select name="includeYear"><option value="true" ${s.includeYear?'selected':''}>是</option><option value="false" ${!s.includeYear?'selected':''}>否</option></select></div><div class="field"><label>加入月份</label><select name="includeMonth"><option value="true" ${s.includeMonth?'selected':''}>是</option><option value="false" ${!s.includeMonth?'selected':''}>否</option></select></div><div class="field full"><div class="notice">會員編號預覽：<b>${s.manualMemberNumber?'人手輸入':nextMemberNumber()}</b></div></div><div class="field full"><button class="btn">儲存設定</button></div></form></section><section class="card"><h2>系統資料備份 / 匯入</h2><p class="sub">可將此裝置的會員資料及設定備份成 JSON 檔，亦可由其他裝置匯入資料。匯入時只會新增資料，不會覆蓋現有資料。</p><div class="actions"><button class="btn" onclick="exportBackup()">下載系統資料備份</button><button class="btn soft" onclick="document.getElementById('importBackupFile').click()">新資料匯入</button><input id="importBackupFile" type="file" accept="application/json,.json" style="display:none"></div><div class="notice warn"><b>注意：</b>如匯入資料出現會員編號重複，系統會先新增檔案並保留原有會員編號，同時標記「編號重複」，請之後到會員資料內自行更改。</div></section>`}}
function memberModal(m){const editable=state.editMode;const cell=(key,type='text')=>editable?`<input name="${key}" ${type==='date'?'type="date"':''} value="${escapeHtml(m[key]||'')}">`:`<div class="readonly-field">${escapeHtml(m[key]||'—')}</div>`;return `<div class="modal-bg"><div class="modal"><div class="top"><div><h2>會員資料</h2><p class="sub">${escapeHtml(m.memberNumber)} · 以登記表格格式顯示</p>${m.duplicateNumber?'<div class="notice warn">此會員由資料匯入新增，會員編號與現有紀錄重複。請按「更改資料」後修改會員編號。</div>':''}</div><button class="btn ghost" onclick="closeMember()">關閉</button></div><form id="editForm" class="formgrid detail-form"><div class="field"><label>會員編號</label>${editable&&state.settings.manualMemberNumber?`<input name="memberNumber" value="${escapeHtml(m.memberNumber||'')}">`:`<div class="readonly-field">${escapeHtml(m.memberNumber)}</div>`}${editable&&!state.settings.manualMemberNumber?`<p class="mini">如需修改會員編號，請先到設定啟用人手更改會員編號。</p>`:``}</div><div class="field"><label>姓名</label>${cell('name')}</div><div class="field"><label>性別</label>${editable?`<select name="gender">${['','男','女','其他/不透露'].map(v=>`<option value="${v}" ${m.gender===v?'selected':''}>${v||'請選擇'}</option>`).join('')}</select>`:`<div class="readonly-field">${escapeHtml(m.gender||'—')}</div>`}</div><div class="field"><label>出生年月日</label>${cell('dateOfBirth','date')}</div><div class="field"><label>年齡</label><div class="readonly-field">${escapeHtml(calcAge(m.dateOfBirth)||m.age||'—')}</div></div><div class="field"><label>身份證號碼</label>${cell('hkid')}</div><div class="field"><label>電話號碼</label>${cell('phone')}</div><div class="field"><label>電郵地址</label>${cell('email')}</div><div class="field full"><label>住址</label>${editable?`<textarea name="address">${escapeHtml(m.address||'')}</textarea>`:`<div class="readonly-field">${escapeHtml(m.address||'—')}</div>`}</div><div class="field"><label>緊急聯絡人姓名</label>${cell('emergencyContactName')}</div><div class="field"><label>緊急聯絡人電話</label>${cell('emergencyContactPhone')}</div><div class="field full"><label>備註</label>${editable?`<textarea name="remarks">${escapeHtml(m.remarks||'')}</textarea>`:`<div class="readonly-field">${escapeHtml(m.remarks||'—')}</div>`}</div><div class="field"><label>申請人姓名</label>${editable?`<input name="applicantName" value="${escapeHtml(m.applicantName||m.name||'')}">`:`<div class="readonly-field">${escapeHtml(m.applicantName||m.name||'—')}</div>`}</div><div class="field"><label>簽署日期</label>${editable?`<input type="date" name="signatureDate" value="${escapeHtml(m.signatureDate||todayISO())}">`:`<div class="readonly-field">${escapeHtml(m.signatureDate||'—')}</div>`}</div><div class="field full"><label>電子簽署</label><div class="readonly-field">${m.signatureImage?`<img src="${m.signatureImage}" style="max-height:72px;max-width:260px;background:white">`:'未有電子簽署'}</div><p class="mini">修改資料會保留原本電子簽署。</p></div><div class="field full actions">${editable?`<button type="submit" class="btn ok">確認修改</button><button type="button" class="btn ghost" onclick="cancelEdit()">取消修改</button>`:`<button type="button" class="btn soft" onclick="startEdit()">更改資料</button><button type="button" class="btn" onclick="downloadPdfById('${m.id}')">下載 PDF</button>`}</div></form><div class="preview-wrap" style="margin-top:16px">${pdfHtml(m)}</div></div></div>`}
function afterRender(){const reg=$('#regForm');if(reg){reg.dateOfBirth?.addEventListener('input',()=>{reg.age.value=calcAge(reg.dateOfBirth.value)});reg.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(reg);const data=Object.fromEntries(fd.entries());data.healthDeclaration=reg.healthDeclaration.checked;data.age=calcAge(data.dateOfBirth);if(!data.name||!data.dateOfBirth||!data.phone){alert('請填寫姓名、出生年月日及電話號碼。');return}data.memberNumber=String(data.memberNumber||'').trim();if(!data.memberNumber){alert('請輸入會員編號。');return}if(state.members.some(m=>m.memberNumber===data.memberNumber)){alert('會員編號已存在，請輸入另一個會員編號。');return}state.draft={...data,id:uid(),applicantName:data.name,signatureDate:todayISO(),createdAt:nowText(),createdAtISO:new Date().toISOString(),updatedAt:nowText()};state.lockedSignature=null;state.page='preview';render()})}
 const set=$('#settingsForm');if(set){set.addEventListener('submit',e=>{e.preventDefault();const fd=Object.fromEntries(new FormData(set).entries());state.settings={...state.settings,...fd,manualMemberNumber:fd.manualMemberNumber==='on',includeYear:fd.includeYear==='true',includeMonth:fd.includeMonth==='true',currentSequence:Math.max(1,parseInt(fd.currentSequence||1))};persist();toast('設定已儲存')})}
 const importInput=$('#importBackupFile');if(importInput){importInput.addEventListener('change',handleImportFile)}
 const ms=$('#memberSearch');if(ms){ms.addEventListener('input',e=>{window._memberSearch=e.target.value;render()})}const mm=$('#memberMonth');if(mm){mm.addEventListener('change',e=>{window._memberMonth=e.target.value;render()})}
 const ef=$('#editForm');if(ef&&state.editMode){ef.addEventListener('submit',e=>{e.preventDefault();if(!verifyPassword('請輸入密碼以確認修改會員資料'))return;const fd=Object.fromEntries(new FormData(ef).entries());const idx=state.members.findIndex(x=>x.id===state.viewMember.id);if(idx>=0){const old=state.members[idx];if(!state.settings.manualMemberNumber){fd.memberNumber=old.memberNumber}else{fd.memberNumber=String(fd.memberNumber||'').trim();if(!fd.memberNumber){alert('請輸入會員編號。');return}if(state.members.some((x,i)=>i!==idx&&x.memberNumber===fd.memberNumber)){alert('會員編號已存在，請輸入另一個會員編號。');return}}const updated={...old,...fd,age:calcAge(fd.dateOfBirth),updatedAt:nowText(),signatureImage:old.signatureImage};state.members[idx]=updated;state.viewMember=updated;state.editMode=false;persist();toast('會員資料已更新')}})}
 if(state.page==='preview')setupSignaturePad();}
let sigCtx=null,sigCanvas=null,drawing=false;
function setupSignaturePad(){sigCanvas=$('#sigPad');if(!sigCanvas)return;const ratio=Math.max(window.devicePixelRatio||1,1);const rect=sigCanvas.getBoundingClientRect();sigCanvas.width=Math.floor(rect.width*ratio);sigCanvas.height=Math.floor(rect.height*ratio);sigCtx=sigCanvas.getContext('2d');sigCtx.scale(ratio,ratio);sigCtx.lineWidth=2.4;sigCtx.lineCap='round';sigCtx.strokeStyle='#111';sigCtx.fillStyle='#fff';sigCtx.fillRect(0,0,rect.width,rect.height);const pos=e=>{const r=sigCanvas.getBoundingClientRect();const t=e.touches?e.touches[0]:e;return{x:t.clientX-r.left,y:t.clientY-r.top}};const start=e=>{e.preventDefault();drawing=true;const p=pos(e);sigCtx.beginPath();sigCtx.moveTo(p.x,p.y)};const move=e=>{if(!drawing)return;e.preventDefault();const p=pos(e);sigCtx.lineTo(p.x,p.y);sigCtx.stroke()};const end=e=>{drawing=false};['mousedown','touchstart'].forEach(ev=>sigCanvas.addEventListener(ev,start,{passive:false}));['mousemove','touchmove'].forEach(ev=>sigCanvas.addEventListener(ev,move,{passive:false}));['mouseup','mouseleave','touchend'].forEach(ev=>sigCanvas.addEventListener(ev,end));}
function clearSignature(){if(sigCtx&&sigCanvas){const rect=sigCanvas.getBoundingClientRect();sigCtx.fillStyle='#fff';sigCtx.fillRect(0,0,rect.width,rect.height);state.lockedSignature=null;render()}}
function confirmSignature(){if(!sigCanvas)return;const name=$('#applicantName').value||state.draft.name;const date=$('#signatureDate').value||todayISO();state.lockedSignature=sigCanvas.toDataURL('image/png');state.draft={...state.draft,applicantName:name,signatureDate:date,signatureImage:state.lockedSignature};render()}
function saveDraftMember(){if(!state.draft)return;if(!String(state.draft.memberNumber||'').trim()){alert('請輸入會員編號。');return}if(!state.draft.signatureImage){alert('請先簽署並按「確認簽署」。');return}const exists=state.members.some(m=>m.memberNumber===state.draft.memberNumber);if(exists){alert('會員編號已存在，請輸入另一個會員編號。');return}state.members.push(state.draft);if(!state.settings.manualMemberNumber){state.settings.currentSequence=(parseInt(state.settings.currentSequence||1)+1)}persist();state.draft=null;state.lockedSignature=null;state.page='members';toast('會員已成功儲存')}
function downloadDraftPdf(){if(!state.draft)return;if(!state.draft.signatureImage){alert('請先簽署並按「確認簽署」，PDF 才會顯示電子簽署。');return}downloadPdf(state.draft)}
function exportBackup(){const data={app:'MemberHub 會員管理系統',version:'v11-backup-import',exportedAt:new Date().toISOString(),settings:state.settings,members:state.members};const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`MemberHub_系統資料備份_${todayISO()}.json`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);toast('系統資料備份已下載')}
function normalizeImportedMembers(raw){if(Array.isArray(raw))return raw;if(raw&&Array.isArray(raw.members))return raw.members;if(raw&&raw.data&&Array.isArray(raw.data.members))return raw.data.members;return []}
function handleImportFile(e){const file=e.target.files&&e.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const parsed=JSON.parse(String(reader.result||''));const incoming=normalizeImportedMembers(parsed);if(!incoming.length){alert('找不到可匯入的會員資料。請確認備份檔格式正確。');e.target.value='';return}let duplicateCount=0;let addedCount=0;const existingNumbers=new Set(state.members.map(m=>String(m.memberNumber||'')));const existingIds=new Set(state.members.map(m=>String(m.id||'')));const imported=incoming.map(item=>{const originalNo=String(item.memberNumber||'').trim()||`IMPORTED-${uid()}`;const isDup=existingNumbers.has(originalNo);if(isDup)duplicateCount++;existingNumbers.add(originalNo);let newId=String(item.id||uid());if(existingIds.has(newId))newId=uid();existingIds.add(newId);addedCount++;return {...item,id:newId,memberNumber:originalNo,duplicateNumber:!!isDup,duplicateOriginalNumber:isDup?originalNo:(item.duplicateOriginalNumber||''),importedAt:nowText(),updatedAt:item.updatedAt||nowText(),createdAt:item.createdAt||nowText(),createdAtISO:item.createdAtISO||new Date().toISOString()}});state.members=[...state.members,...imported];persist();state.page='members';render();alert(`匯入完成：新增 ${addedCount} 份會員檔案。${duplicateCount?`
其中 ${duplicateCount} 份出現會員編號重複，已先新增並標記「編號重複」，請自行更改。`:''}`)}catch(err){console.error(err);alert('匯入失敗：檔案不是有效 JSON 或格式不正確。')}finally{e.target.value=''}};reader.readAsText(file,'utf-8')}
function clearRegister(){state.draft=null;render()}
function openMember(id){if(!verifyPassword('請輸入密碼以查看會員資料'))return;state.viewMember=state.members.find(m=>m.id===id);state.editMode=false;render()}
function closeMember(){state.viewMember=null;state.editMode=false;render()}
function startEdit(){state.editMode=true;render()}
function cancelEdit(){state.editMode=false;render()}
function deleteMember(id){if(!verifyPassword('請輸入密碼以刪除會員'))return;if(!confirm('確定刪除此會員？此操作不能復原。'))return;state.members=state.members.filter(m=>m.id!==id);persist();toast('會員已刪除')}
function downloadPdfById(id){const m=state.members.find(x=>x.id===id);if(m)downloadPdf(m)}
window.exportBackup=exportBackup;window.handleImportFile=handleImportFile;window.setPage=setPage;window.clearRegister=clearRegister;window.clearSignature=clearSignature;window.confirmSignature=confirmSignature;window.saveDraftMember=saveDraftMember;window.downloadDraftPdf=downloadDraftPdf;window.openMember=openMember;window.closeMember=closeMember;window.startEdit=startEdit;window.cancelEdit=cancelEdit;window.deleteMember=deleteMember;window.downloadPdfById=downloadPdfById;window.downloadPdf=downloadPdf;
render();
